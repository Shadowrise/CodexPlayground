{
  "name": "kirby-game-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "wrangler types && tsc --noEmit"
  },
  "devDependencies": { "typescript": "^5.9.2", "wrangler": "^4" }
}
